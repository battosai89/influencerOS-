import * as React from 'react';

const Inbox: React.FC = () => {
    return (
        <div>
            <h1 className="text-3xl font-bold text-brand-text-primary">Inbox</h1>
            <p className="text-brand-text-secondary mt-4">This is the Inbox page. Content to be added.</p>
        </div>
    );
};

export default Inbox;
